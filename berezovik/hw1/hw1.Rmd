---
title: "Homework #1"
author: "Berezovik"
date: '2 октября 2017 г '
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

**Задание 1**


- Загрузка данных
```{r}
data.df<-read.table("http://www.stats.uwo.ca/faculty/braun/data/rnf6080.dat", header = TRUE)

```
- Получение количества строк
```{r}
print(nrow(data.df))
```
- Получение количества столбцов
```{r}
print(ncol(data.df))
```
- Получение имен столбцов
```{r }
colnames(data.df)
data.df.colnames <- colnames(data.df)
```
- Нахождение значения из 5 строки седьмого столбца.
```{r }
data.df[5,7]
```
- Вывод 2 строки из data.df
```{r }
print (data.df[2,])
```


- Объясните, что делает следующая строка кода names(data.df) <- c("year", "month", "day", seq(0,23)). Воспользуйтесь функциями head и tail, чтобы просмотреть таблицу. Что представляют собой последние 24 колонки?*

```{r }
names(data.df) <- c("year", "month", "day", seq(0,23))
head(data.df)
```
```{r }
tail(data.df)
```
    
Данная функция изменяет имена колонок. Последние 24 колонки data.df представляют собой       данные за 80ый год 25-30 Ноября. Значения равны нулю, осадков не было.
    

- Добавьте новую колонку с названием daily, в которую запишите сумму крайних правых 24 колонок. Постройте гистограмму по этой колонке. Какие выводы можно сделать?

```{r}
data.df["daily"] <- rowSums(data.df[(length(data.df.colnames)-23):length(data.df.colnames)])
hist(data.df$daily) 
```   
  
    Гистограмма показывает отрицательные значения, что есть не очень хорошо.

- Создайте новый датафрейм fixed.df в котром исправьте замеченную ошибку. Постройте новую гистограмму, поясните почему она более корректна.
```{r}
fixed.df = data.df
fixed.df[["daily"]] = abs(fixed.df[["daily"]])
hist((fixed.df[["daily"]]))
```

**Задание 2**

- Для каждой строки кода поясните полученный результат, либо объясните почему она ошибочна.
```{r}
v <- c("4", "8", "15", "16", "23", "42")
max(v)
var(v)
#sum(v)
```
*max - неверное, var- верно, sum - не считает. Ниже приведен пример корректной работы данных функций.*

```{r}
x <- c(4,8,15,16,23,42)
max(x)
var(x)
sum(x)
```

- Для следующих наборов команд поясните полученный результат, либо объясните почему они ошибочна.

```{ }
#v2 <- c("5",7,12)
#v2[2] + 2[3]
```
Невозможно суммирование элементов вектора типа char, так как + работате только с числовыми аргументами.
```{ }
df3 <- data.frame(z1="5",z2=7,z3=12)
df3[1,2] + df3[1,3]
```
Data Frame поддерживает работу с разными типами данными, поэтому в данном случае выполнится сложение.
```{}
l4 <- list(z1="6", z2=42, z3="49", z4=126)
l4[[2]] + l4[[4]]
l4[2] + l4[4]
```
В списках (листах) оператор [] - выводит список элементов по индексу, а [[]] - сам элемент, поэтому в первом случае выполнится сложение, а во втором - отобразится ошибка

**Работа с функциями и операторами**


  - Вывод на экран:
  - Числа от 1 до 10000 с инкрементом 372.
  - Числа от 1 до 10000 длиной 50.

```{r }
seq(1, 10000, by = 372)
seq(1, 10000, length.out=50)
```

- Функция rep() повторяет переданный вектор указанное число раз. Объясните разницу между rep(1:5,times=3) и rep(1:5, each=3).
```{r }
rep(1:5,times=3)
rep(1:5, each=3)
``` 
  Разница заключается в том, что в первом случаи последовательность чисел от 1 до 5            повторяется 3 раза друг за другом, а во втором случаи каждое число от 1 до 5 повторяется     сразу по 3 раза*